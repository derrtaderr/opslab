# Miro Configuration
NEXT_PUBLIC_MIRO_APP_ID=your_miro_app_id
MIRO_CLIENT_SECRET=your_miro_client_secret
NEXT_PUBLIC_MIRO_BOARD_ID=your_template_board_id

# Note: The client secret should NEVER be exposed to the frontend
# Only use NEXT_PUBLIC_ prefix for values that need to be available in the browser

# App Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret  # Generate with: openssl rand -base64 32

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# OpenAI Configuration (for later use)
OPENAI_API_KEY=your_openai_api_key 